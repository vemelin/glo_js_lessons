'use strict';

const money = 0, 
      income = 0, 
      addExpenses = 0, 
      deposit = 0, 
      mission = 0, 
      period = 30;

    console.log(document.cookie = secure: true, 'max-age': 3600);
    

//setInterval(function(){ alert("Hello"); }, 3000);